import React, {component} from 'react';

export default class FormResult extends Component{

    getTotalTime=(props)=>{

    }

    // Return each items minutes and description in a list
    getOrderedItemList=(props)=>{
        this.props.
    }

    render(){
        return(
            <div>
                <h1>{this.props.project}</h1>
                <h3>{this.getTotalTime()}</h3>
                {itemList}
                
            </div>
        );
    }
}